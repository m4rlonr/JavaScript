// Este script exibe uma mensagem no console
console.log("Olá, Mundo!");

// Função simples para somar dois números
function somar(a, b) {
  return a + b;
}

// Exemplo de uso da função
let resultado = somar(3, 4);
console.log(`A soma de 3 e 4 é: ${resultado}`);
